{
  "// description": "DOM-context scripts: the content scripts (trigger, overlay), the shared helpers, and the offscreen document. All classic scripts (no modules).",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist-page",
    "rootDir": "./src",
    "lib": ["ES2020", "DOM", "DOM.Iterable"]
  },
  "files": [
    "src/chrome-api.d.ts",
    "src/ext-common.ts",
    "src/trigger.ts",
    "src/overlay.ts",
    "src/offscreen.ts"
  ]
}
